Production Android identity and signing
Your Android app still uses com.example.africa_beuty and debug signing in android/app/build.gradle.kts (line 27).
This is a launch blocker. Set a real package name, release keystore, and release signing config.

Secure token storage
Auth tokens are stored in SharedPreferences in local_storage_service.dart (line 31).
Before launch, move access/refresh tokens to flutter_secure_storage or platform secure storage.

Production backend/environment config
API URL is hardcoded to Render in server_constants.dart (line 6).
Add proper dev/staging/prod environment configuration, and confirm the production backend is stable, HTTPS-only, backed up, and monitored.

Crash/error monitoring fully enabled
You already have ErrorMonitor, but Bugfender only works if BUGFENDER_APP_KEY is passed at build time in error_monitor.dart (line 10).
First launch needs real crash reporting, backend error reporting, and a way to see failures quickly.

Permissions review
Android requests location, notifications, exact alarms, and boot completed in AndroidManifest.xml (line 2).
Make sure each permission is truly needed, explained in the UI, and supported by privacy policy/store declarations.

Core launch flows must be tested end-to-end
The must-pass flows are: onboarding, sign up, verify account, login, logout/session expiry, profile setup, salon/customer profile, search, create post, view post, booking, notifications, reminders, image upload, and offline/timeout behavior.

Store-ready branding
App icon/splash mostly exists, but APP_ICON_SPLASH_REVIEW.md (line 15) says a dedicated brand splash asset is still needed. Also update README/package metadata because it still says “A new Flutter project.”

Analyzer cleanup
flutter analyze currently reports 44 issues: mostly deprecated withOpacity, plus a few unused declarations. Not a huge blocker, but clean these before release so real problems don’t hide in noise.

If I had to pick the “important one” first: fix Android package/signing and secure auth token storage. Those are the two I would not launch without.




Rotate and remove committed secrets
Backend .env is tracked by git, and secrets/default credentials also appear in core/database.py (line 74) and send_code.py (line 12).
This is the biggest backend launch blocker. Rotate DB, R2, JWT, email, Firebase credentials, remove .env from git history if this repo has ever been pushed, and require env vars in production.

Secure frontend token storage
Frontend still stores auth tokens in shared preferences. Move access/refresh tokens to secure storage before launch.

Production app identity and signing
Android still uses com.example.africa_beuty and debug signing. This must be fixed before Play Store/testing release.

Production environment config
Frontend is hardcoded to the Render backend, and backend has fallback production-looking URLs. Both sides need clean dev/staging/prod config with no secret fallbacks.

Fix the refresh endpoint typo carefully
Backend route is /auth/refersh in routes/auth.py (line 97), and frontend calls the same typo. Add the correct /auth/refresh endpoint while keeping /refersh temporarily for old app builds.

Lock down API exposure
Backend currently includes routers twice: /api/v1/... and also root compatibility routes in main.py (line 57). Decide if root routes are truly needed for launch. If not, remove them to reduce accidental public surface.

Enable production monitoring
Backend has Sentry support and JSON logs. Frontend has Bugfender/backend crash reporting. Make sure SENTRY_DSN, BUGFENDER_APP_KEY, health checks, and alerting are actually configured in production.

Run full end-to-end launch flows
Especially auth, verification email, token refresh/logout, image uploads to R2, booking, notifications/FCM, reminders, posts, search, and profile setup.

Good news: backend tests pass: 21 passed, 1 warning. Frontend analyzer still has 44 issues, mostly deprecated withOpacity plus unused declarations. Backend is structurally closer than I expected, but the committed secrets/fallback credentials are the loudest “do this before launch” item.


User choice to select the own time for promotions and events.



<!-- Hope so -->
Best Things To Add Next

Public Profile Preview Mode
Let customer/salon see exactly how their profile looks to other people.
“View as public”
“View as customer”
“View as salon owner”
Show hidden/visible labels based on privacy settings



Role-Aware Profile Dashboard
Instead of only settings/tools, add a profile “command center”.
Customer:
Upcoming booking
Saved salons
Beauty preferences
Recommendation status
Recent profile activity
Salon:
Today’s bookings
Pending requests
Profile reach
Top service
Draft campaigns
Staff access alerts



Deep Link Handling
You added share links, but next is making the app open them:
/u/{username} opens customer profile
/s/{username} opens salon profile
Fallback web preview if app not installed




The strongest next move is Public Profile Preview + Profile Completion Score + backend enforcement of saved settings. Those will make the profile feel less like “settings screens” and more like a real production-grade account system.

Later, if you want richer ratings, you can add optional categories:
Service quality
Cleanliness
Time keeping
Stylist skill
Value for money


Notifications page
Important for booking trust: confirmed, rejected, completed, review request, stylist assigned, rescheduled.
Need clean copy and correct navigation when tapped.
My strongest next pick: Create Post page, because salon content quality powers the whole app.


Best next moves, in order:

Create Post quality guide
Add subtle checks/copy for “clear service photo, service selected, useful caption, hashtags, tag stylist/client where relevant.”

Post preview before publish
Let salon see how the post will appear in feed/profile before submitting.

Draft/save recovery
If they leave mid-post, keep caption/images/settings so content is not lost.

Better post success flow
After posting: “View post”, “Create another”, “Share”, maybe “Boost later”.

Notifications tap polish follow-up
If backend sends exact notification types for stylist assigned/review request, confirm frontend recognizes all names.

My pick: Post preview before publish + better success flow. It’s visible, high-impact, and makes salon content creation feel much more professional.